# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Admin Authentication (CRITICAL: Change this from default!)
# Generate a strong password with at least 16 characters
ADMIN_PASSCODE=your_secure_admin_password_here

# Optional: Tempo Development Tools
NEXT_PUBLIC_TEMPO=false

# Production Environment
NODE_ENV=production

# Security Notes:
# 1. NEVER commit .env files to version control
# 2. Use strong, unique passwords for ADMIN_PASSCODE
# 3. Rotate credentials regularly
# 4. Use different credentials for development/staging/production 